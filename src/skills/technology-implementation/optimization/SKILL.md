---
name: optimization-qualification
description: >
  Qualify an optimization opportunity (improve, tune, remediate, or modernize a system the
  organization already runs in production). Use when the scope is about making an existing
  system work better rather than building a new one or operating it ongoing.
---

# Optimization Qualification

Loaded when the classifier labels an opportunity `optimization`. Qualify, read the
(usually fixed-scope) deal shape, and compile what the **optimization practice** needs.

## When this applies
Improving an installed base: performance tuning, process redesign on an existing system,
remediation, modernization, health checks. The system is already live.

## Qualification rubric  `[FILL IN — your domain judgment]`
- Is the existing system and the pain/opportunity clearly stated? `[FILL IN]`
- Optimization fit with the firm's capability? `[FILL IN]`
- Is the value of fixing it sized or implied? `[FILL IN]`
- Disqualifiers: `[FILL IN]`

## ICP signals specific to optimization
On top of the general ICP: strong optimization fit tends to look like `[FILL IN — e.g. system
in prod but underperforming, recent implementation that struggled, audit/health-check trigger]`.

## Deal-shape heuristics
Read and report: typically a bounded assessment + remediation; fixed-fee phases.
`[FILL IN your typical optimization deal shapes]`

## Route to & package to compile
**Target team:** optimization practice lead.
Compile into `team_package`:
- `current_system`: the system in production
- `pain_points`: stated problems / optimization targets
- `assessment_scope`: what a first-phase assessment would cover
- `value_hypothesis`: the upside of optimizing, if implied
- `open_questions`

## Readout template
Verdict sentence first, then the assessment-shaped deal, then the package below.
